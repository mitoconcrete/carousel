const elScrollContainer = document.getElementById(
  "container__carousel--scrolling"
);

const mouseEnter = {
  enter: 0,
  leave: 0,
  type: "STAY",
};

let position = 0;

console.log(elScrollContainer);

// setInterval(() => {
//   position += 100;
//   elScrollContainer.style.transform = "translateX(-" + position + "%)";
// }, 1000);

elScrollContainer.addEventListener("onmouseup");
elScrollContainer.addEventListener("onmouseup");
